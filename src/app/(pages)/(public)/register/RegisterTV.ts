import { tv } from 'tailwind-variants';

export const registerTv = tv({
  base: 'flex flex-col min-[1520px]:flex-row max-[1520px]:items-center max-[1520px]:justify-center relative animate-opacityOn'
});
