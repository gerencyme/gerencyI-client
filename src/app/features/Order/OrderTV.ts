import { tv } from 'tailwind-variants';

export const orderRootTv = tv({
  base: 'flex flex-col gap-4 lg:gap-8 max-w-7xl m-auto py-8'
});

export const orderLastProductsTv = tv({
  base: 'h-full max-h-[520px] relative'
});
