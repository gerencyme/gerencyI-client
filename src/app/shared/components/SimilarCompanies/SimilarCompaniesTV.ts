import { tv } from 'tailwind-variants';

export const similarCompaniesRootTv = tv({
  base: 'relative py-0.5 md:py-3 w-full h-fit flex flex-col justify-center gap-1.5 md:gap-3'
});

export const similarCompaniesContentListTv = tv({
  base: 'flex items-center gap-1.5 md:gap-3'
});

export const similarCompaniesDivisorTv = tv({
  base: 'h-0.5 w-full bg-lightPurple'
});
