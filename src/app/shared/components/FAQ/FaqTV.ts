import { tv } from 'tailwind-variants';

export const faqRootTv = tv({
  base: 'xl:absolute md:right-12 top-1/4 py-1 md:py-4 flex flex-col items-center justify-center gap-8 cursor-default m-auto w-full md:max-w-2xl'
});

export const FaqContentTv = tv({
  base: 'flex flex-col h-full'
});
