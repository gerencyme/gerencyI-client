import { FilterIcon } from './views/FilterIcon';
import { FilterLabel } from './views/FilterLabel';
import { FilterRoot } from './views/FilterRoot';

export const Filter = {
  root: FilterRoot,
  label: FilterLabel,
  icon: FilterIcon
};
