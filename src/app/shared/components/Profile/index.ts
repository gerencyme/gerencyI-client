import { ProfileImage } from './views/ProfileImage';
import { ProfileInfo } from './views/ProfileInfo';
import { ProfileRoot } from './views/ProfileRoot';

export const Profile = {
  root: ProfileRoot,
  avatar: ProfileImage,
  info: ProfileInfo
};
