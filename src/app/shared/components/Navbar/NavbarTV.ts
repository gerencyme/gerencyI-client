import { tv } from 'tailwind-variants';

export const navbarRootTv = tv({
  base: 'flex gap-4'
});

export const navbarRenderDropdownTv = tv({
  base: 'flex items-center gap-4'
});

export const navbarRenderDropdownIconTv = tv({
  base: 'absolute -top-1 -right-1 z-50'
});
